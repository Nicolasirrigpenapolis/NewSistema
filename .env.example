# =============================
# Variáveis de Ambiente - MDFe Sistema
# Renomeie para .env e ajuste valores seguros em produção.
# =============================

# Banco de Dados (Multi-Empresa - Cada empresa tem seu próprio banco)
SA_PASSWORD=ChangeMe123!
# IMPORTANTE: O nome do banco é definido na seção "Empresa.StringConexao" do appsettings.json
# Exemplos: SistemaDesenvolvimento, SistemaEmpresa01, SistemaEmpresa02, etc.
MDFE_DB_CONN=Server=sqlserver,1433;Database=SistemaDesenvolvimento;User Id=sa;Password=ChangeMe123!;TrustServerCertificate=True;Encrypt=False;MultipleActiveResultSets=True;Connection Timeout=15;Pooling=true;Min Pool Size=5;Max Pool Size=100;
MDFE_SQL_EXTERNAL_PORT=14333

# API / Auth
ASPNETCORE_ENVIRONMENT=Development
JWT_SECRET_KEY=DevSecretKeyChangeMe
API_PORT=8080

# Seed / Dados iniciais
SEED_DATABASE=1
# SUPERUSER_USERNAME pode ser definido opcionalmente para liberar bypass temporário (ex.: SUPERUSER_USERNAME=admin)

# Frontend
FRONTEND_PORT=3000
BUILD_FRONTEND=1

# Observações:
# - Este sistema é MULTI-EMPRESA com BANCOS DE DADOS SEPARADOS
# - Cada empresa tem sua própria string de conexão definida em appsettings.json → Empresa.StringConexao
# - O banco pode ter nomes como: SistemaEmpresa01, SistemaEmpresa02, SistemaCliente01, etc.
# - A variável MDFE_DB_CONN sobrescreve a string do appsettings.json se definida
# - Ajuste Encrypt=True em MDFE_DB_CONN para produção e forneça certificado.
# - Troque todas as senhas antes de expor o ambiente.
# - Para criar usuário administrador utilize o script backend/Scripts/CreateAdminUser.sql ou outro processo controlado.
